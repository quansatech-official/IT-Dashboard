server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location / {
        try_files $uri /index.html;
    }

    location /mobil/ {
        alias /usr/share/nginx/html/;
        try_files $uri /index.html;
    }
}
